{% extends 'base.html.twig' %}

{% block title "Applications | ATOS" %}

{% block body %}
    {# {{form_start(form)}}
        {{form_widget(form)}}
        <input type="button" value="submit">
    {{form_end(form)}} #}

{% endblock %}
